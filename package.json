{
  "name": "node-red-contrib-google-analytics",
  "version": "0.0.2",
  "description": "Google Analycitsのノードです。",
  "main": "app.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/n0bisuke/node-red-contrib-google-analytics.git"
  },
  "scripts": {
    "git-push": "git add -A && git commit -m 'update' && git push origin master",
    "deploy": "npm run git-push && npm publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "node-red": {
    "nodes": {
      "Realtime": "./nodes/realtime/realtime.js"
    }
  },
  "keywords": [
    "Google Analytics",
    "Google",
    "node-red",
    "GA"
  ],
  "author": "n0bisuke",
  "license": "Apache 2.0",
  "dependencies": {
    "@google-analytics/data": "^2.8.0"
  }
}
